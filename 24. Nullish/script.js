let data = "gie";

if (data == null || data == undefined) {
    data = "gajah"
}

console.log(data);

let parameter;

let data1 = parameter ?? "Nilai default"; //nulish coalising operator digunakan untuk mengecek apakah null atau undefined

console.log(data1);